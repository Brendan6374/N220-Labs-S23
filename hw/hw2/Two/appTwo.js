function setup() {
    createCanvas(400, 300);
    x = mouseX
    y = mouseY
}

function draw(){
      
    
    circle (mouseX, mouseY, 20)
    fill(224, 46, 43)
 if (mouseX < 200) {
    fill(52, 67, 235)
}

}