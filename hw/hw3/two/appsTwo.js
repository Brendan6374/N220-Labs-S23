function setup() {
    createCanvas(800, 600)
   
}

function draw(){
    
    let c = color (170, 200, 150)
    c.setRed(0)
    circle(400, 300, 50);
    fill(c);
    stroke(c);

    
    
}
// let c equal the color so that it can be used as a variable 
